# Review is requested automatically from the owners of a changed path.
# Teams must be written as @org/team — a bare @name is read as a user, and an
# unresolvable owner silently disables the rule rather than failing loudly.

* @Wikia/TVMOV

# The detector and the storage it depends on. A change here can degrade results
# without failing a build: run `npm run backtest` and compare the lift before
# approving, because CI cannot (it hits Wikipedia live).
/src/buzz.ts       @Wikia/TVMOV
/src/signals.ts    @Wikia/TVMOV
/src/store.ts      @Wikia/TVMOV
/src/remote.ts     @Wikia/TVMOV
/src/publish.ts    @Wikia/TVMOV

# Scheduling, secrets and the daily job.
/Jenkinsfile-daily-scan  @Wikia/TVMOV
/.github/workflows/      @Wikia/TVMOV
